import type {
  BallOptions,
  BoxOptions,
  Camera,
  Quaternion,
  SolidColor,
  Transform,
  TubeOptions,
  Vector3,
} from "@webgame/engine";

export type SomeType = number;

export function something(): void {
  // do nothing
}