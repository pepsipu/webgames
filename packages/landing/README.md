# @webgame/landing

Landing game rules on top of `webge` abstractions.

## Scope

- Defines world setup for landing (floor + controllable ball).
- Defines input-to-motion behavior for landing.
- Uses `webge` input + physics abstractions.

No WASM exports, networking, or transport concerns live here.
