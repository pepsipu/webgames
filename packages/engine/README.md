the role of the engine is to:

- execute systems
- maintain the element tree
- provide interfaces and registries for elements to define:
- - script bindings
- - serialization (replication logic is actually in network)
