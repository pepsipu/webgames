{
  "name": "webgames",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel --filter @webgames/server --filter @webgames/client dev",
    "build": "pnpm -r build",
    "benchmark:client": "node ./scripts/benchmark-client.mjs",
    "graph:deps": "node ./scripts/dependency-graph.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "simple-git-hooks",
    "serve": "pnpm build && pnpm -r --parallel --filter @webgames/server --filter @webgames/client serve",
    "preview": "pnpm --filter @webgames/client preview"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm format"
  },
  "devDependencies": {
    "@prettier/plugin-xml": "^3.4.2",
    "@viz-js/viz": "^3.25.0",
    "playwright": "^1.58.2",
    "prettier": "^3.6.2",
    "simple-git-hooks": "^2.13.1"
  }
}
