{
  "name": "webgame",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm --filter @webgame/client dev",
    "build": "pnpm -r build",
    "preview": "pnpm --filter @webgame/client preview"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.69"
  }
}
