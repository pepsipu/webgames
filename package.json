{
  "name": "webgame",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel --filter @webgame/server --filter @webgame/client dev",
    "build": "pnpm -r build",
    "serve": "pnpm build && pnpm -r --parallel --filter @webgame/server --filter @webgame/client serve",
    "preview": "pnpm --filter @webgame/client preview"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.69"
  }
}
