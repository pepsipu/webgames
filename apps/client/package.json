{
  "name": "@webgame/client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "pnpm --filter @webgame/webge build:wasm:dev",
    "dev": "vite",
    "prebuild": "pnpm --filter @webgame/webge build:wasm",
    "build": "tsc --project tsconfig.json --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.66",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@webgame/shared": "workspace:*",
    "@webgame/webge": "workspace:*"
  }
}
